{
  "private": true,
  "workspaces": [
    "./packages/@contentlayer/*",
    "./packages/*",
    "./packages/playground-azimuth-sanity/*"
  ],
  "scripts": {
    "test": "jest",
    "build": "yarn workspaces foreach --verbose --topological-dev --no-private run build",
    "release:pre": "yarn workspaces foreach --verbose --topological-dev --no-private version prerelease && yarn workspaces foreach --verbose --topological-dev --no-private npm publish --tolerate-republish"
  },
  "devDependencies": {
    "@types/jest": "^26.0.23",
    "jest": "^26.6.3",
    "ts-jest": "^26.5.6",
    "ts-node": "^9.1.1",
    "typescript": "^4.2.4"
  },
  "resolutions": {
    "contentlayer": "workspace:*",
    "contentlayer-stackbit-yaml-generator": "workspace:*",
    "next-dev-contentlayer": "workspace:*"
  }
}
