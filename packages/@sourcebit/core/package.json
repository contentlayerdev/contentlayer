{
  "name": "@sourcebit/core",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "type": "module",
  "scripts": {
    "dev": "rm -rf dist tsconfig.tsbuildinfo && tsc --watch",
    "build": "rm -rf dist tsconfig.tsbuildinfo && tsc"
  },
  "devDependencies": {
    "typescript": "^4.1.5"
  },
  "dependencies": {
    "rxjs": "^6.6.6"
  }
}
