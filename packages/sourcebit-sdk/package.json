{
  "name": "@sourcebit/sdk",
  "main": "./dist/index.js",
  "type": "module",
  "scripts": {
    "dev": "rm -rf dist *.tsbuildinfo && tsc --watch",
    "build": "rm -rf dist *.tsbuildinfo && tsc"
  },
  "devDependencies": {
    "typescript": "^4.1.5"
  }
}
