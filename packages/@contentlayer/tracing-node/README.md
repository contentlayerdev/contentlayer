# @contentlayer/tracing-node

This package is intended to be used by "application entrypoints" (e.g. the Contentlayer CLI or `next-contentlayer` plugin) as it sets up and initiates the Open Telemetry tracing.
