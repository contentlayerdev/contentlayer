{
  "name": "contentlayer-stackbit-yaml-generator",
  "version": "0.0.1",
  "bin": "./dist/index.js",
  "scripts": {
    "prepack": "yarn build",
    "build": "yarn build:cli && yarn build:lib",
    "build:cli": "rm -rf dist && esbuild --bundle src/index.ts --outfile=dist/index.js --platform=node --sourcemap --external:esbuild"
  },
  "dependencies": {
    "@contentlayer/core": "workspace:*",
    "@stackbit/sdk": "^0.1.9",
    "clipanion": "^3.0.0-rc.11",
    "yaml": "^1.10.2"
  },
  "devDependencies": {
    "@types/yaml": "^1",
    "esbuild": "^0.11.5",
    "typescript": "^4.2.3"
  }
}
